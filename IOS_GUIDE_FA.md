# راهنمای جامع نسخه iOS اپلیکیشن ای‌پلاک (آیفون)

سامانه **ای‌پلاک** اکنون به صورت دوگانه برای دستگاه‌های آیفون و سیستم‌عامل iOS آماده و بهینه‌سازی شده است:
1. **پروژه نیتیو استاندارد Xcode (Swift + WKWebView)**
2. **وب‌اپلیکیشن پیش‌رونده (PWA)** با پشتیبانی کامل از حالت تمام‌صفحه و آفلاین در Safari iOS

---

## ۱. ساخت خودکار فایل خروجی IPA از گیت‌هاب (بدون نیاز به مک)

یک گردش‌کار اختصاصی GitHub Actions به نام `Build iOS App (IPA)` در مسیر `.github/workflows/ios-build.yml` قرار داده شده است.

### مراحل دریافت فایل IPA:
1. وارد صفحه ریپازیتوری خود در گیت‌هاب (`hosseinbahrami-bot/eplak`) شوید.
2. روی تب **Actions** در بالای صفحه کلیک کنید.
3. از منوی سمت چپ، ورک‌فلو **Build iOS App (IPA)** را انتخاب نمایید.
4. دکمه **Run workflow** را بزنید (یا با هر کامیت جدید روی برنچ `main` به صورت خودکار اجرا می‌شود).
5. سرورهای ابری مکینتاش گیت‌هاب پروژه را کامپایل کرده و در انتهای اجرا، فایل **`Eplak-iOS-IPA`** را در بخش **Artifacts** برای دانلود در اختیارتان می‌گذارند.

---

## ۲. روش‌های نصب فایل IPA روی آیفون

پس از دانلود فایل `Eplak.ipa` می‌توانید با یکی از ابزارهای زیر آن را مستقیماً روی گوشی نصب کنید:

### الف) برنامه Sideloadly (ویندوز یا مک):
1. نرم‌افزار رایگان **Sideloadly** را روی رایانه نصب کنید.
2. آیفون را با کابل به کامپیوتر متصل کنید.
3. فایل `Eplak.ipa` را داخل برنامه بکشید (Drag & Drop).
4. با وارد کردن Apple ID خود، دکمه **Start** را بزنید تا برنامه روی گوشی نصب شود.
5. در آیفون به مسیر `Settings > General > VPN & Device Management` بروید و روی ایمیل خود `Trust` را بزنید.

### ب) برنامه AltStore یا Scarlet:
- ابزارهای سایدلود مستقیم روی گوشی که بدون نیاز به کابل نیز اپ را فعال نگه می‌دارند.

---

## ۳. باز کردن و خروجی گرفتن با Xcode در سیستم مک

اگر خودتان یا تیم‌تان به سیستم‌عامل macOS و نرم‌افزار Xcode دسترسی دارید:
1. ریپازیتوری را دانلود کنید.
2. وارد پوشه `ios-app/` شوید و فایل **`Eplak.xcodeproj`** را باز کنید.
3. در بخش **Signing & Capabilities**، تیم توسعه خود (Personal Team یا Apple Developer) را انتخاب کنید.
4. گوشی آیفون متصل شده یا Simulator مورد نظر را انتخاب و دکمه **Run (⌘R)** را فشار دهید.
5. برای خروجی نهایی: از منوی بالای Xcode گزینه **Product > Archive** را انتخاب کنید.

---

## ۴. انتشار در استورهای ایرانی iOS (سیبچه، اناردونی، سیب‌اپ)

برای انتشار اپ در مارکت‌های ایرانی، نیازی به اکانت دلاری دولوپر اپل ندارید:
1. فایل `Eplak.ipa` بیلد شده را آماده کنید.
2. در پنل توسعه‌دهندگان مارکت مورد نظر (مثلاً Sibche یا Anardoni) ثبت نام کنید.
3. فایل IPA و اطلاعات سامانه (عنوان «ای‌پلاک»، آیکون‌های موجود در `ios-app/Eplak/Assets.xcassets` و توضیحات) را بارگذاری نمایید تا با گواهی سازمانی (Enterprise) یا Ad-Hoc منتشر شود.

---

## ۵. نسخه وب‌اپلیکیشن PWA (نصب فوری و رایگان در ۳ ثانیه)

این روش رایج‌ترین و محبوب‌ترین راهکار برای سرویس‌های داخل کشور در سیستم‌عامل iOS است:
1. آدرس سایت ای‌پلاک را در مرورگر **Safari** آیفون باز کنید.
2. در نوار پایین سافاری، دکمه **اشتراک‌گذاری (Share Icon ⎋)** را لمس کنید.
3. به سمت پایین اسکرول کرده و گزینه **افزودن به صفحه اصلی (Add to Home Screen)** را انتخاب کنید.
4. روی **Add (افزودن)** در بالا سمت راست بزنید.
5. آیکون اختصاصی ای‌پلاک به صفحه برنامه‌های آیفون اضافه می‌شود و با باز کردن آن، برنامه به صورت **تمام‌صفحه (Standalone)** و بدون نوار آدرس یا دکمه‌های مرورگر، دقیقاً مانند یک اپلیکیشن نیتیو اپ‌استور اجرا می‌شود.
