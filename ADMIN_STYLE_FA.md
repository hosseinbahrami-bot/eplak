# به‌روزرسانی ظاهر پنل ادمین: فونت فارسی، لوگو و حالت شب/روز

**تاریخ:** ۷ سپتامبر ۲۰۲۶

---

## ۱. فونت‌های فارسی

سه فونت درخواستی در یک زنجیره (Font Stack) چیده شدند تا هرکدام در دسترس باشد، همان به‌کار برود:

| کاربرد | اولویت فونت‌ها |
|---|---|
| متن‌ها | کلمه ← **ایران‌سنس** ← یکان ← تاهوما |
| عنوان‌ها | کلمه ← **یکان** ← ایران‌سنس ← تاهوما |

```
--font-family:  'Kalameh', 'IranianSans', 'YekanBakh', 'Tahoma', ...
--font-heading: 'Kalameh', 'YekanBakh', 'IranianSans', 'Tahoma', ...
```

### وضعیت هر فونت

| فونت | وضعیت | توضیح |
|---|---|---|
| **یکان (YekanBakh)** | ✅ نصب‌شده | ۵ وزن: Light، Regular، Medium، Bold، Heavy — درون پروژه |
| **ایران‌سنس (IranianSans)** | ✅ نصب‌شده | ۲ وزن (Regular و Bold) — نسخه آزادِ ایرانیان‌سنس |
| **کلمه (Kalameh)** | ⏳ در انتظار | **تجاری** (فقط از فونت‌ایران) و امکان دانلود قانونی ندارد |

### نکته مهم درباره فونت «کلمه»

فونت کلمه اثر سید ناصر خادم، **پولی** است و نسخه رایگان ندارد؛ به همین دلیل امکان قرار دادن آن در پروژه وجود نداشت. با این حال نام آن در **ابتدای زنجیره فونت** قرار گرفته، بنابراین:

- اگر روی سیستم شما نصب باشد → خودکار استفاده می‌شود.
- اگر فایل‌هایش را بفرستید یا در `admin/assets/fonts/` قرار دهید → با یک خط `@font-face` فعال می‌شود.

تا آن زمان، متن‌ها با ایران‌سنس و عنوان‌ها با یکان نمایش داده می‌شوند.

**فایل‌های افزوده‌شده:**
```
admin/assets/fonts/YekanBakh-{Light,Regular,Medium,Bold,Heavy}.woff2
admin/assets/fonts/IranianSans-{Regular,Bold}.woff2
```

## ۲. جایگزینی لوگو

تصویر ارسالی (واژه‌نشان با پس‌زمینه شفاف) پردازش و در دو نسخه ذخیره شد:

```
admin/assets/img/logo.png        ← نسخه اصلی (برای حالت روز)
admin/assets/img/logo-light.png  ← نسخه روشن (برای حالت شب)
```

- برش خودکار تا لبه محتوا، تغییر اندازه به ۹۰۰ پیکسل عرض، حفظ شفافیت
- جایگزینِ آیکون قدیمی در **صفحه ورود** و عنوان **«ای‌پلاک Admin» در سایدبارِ ۱۸ صفحه**
- در حالت شب، نسخه روشن به‌طور خودکار جایگزین می‌شود

یک نسخه روشن هم ساخته شد چون رنگ اصلی لوگو تیره است و روی زمینه تیره خوانا نبود.

## ۳. حالت شب و روز

پیش از این، حالت تاریک فقط از تنظیم سیستم پیروی می‌کرد و راهی برای تغییر دستی نداشت. اکنون:

- یک **کلید «حالت شب / حالت روز»** بالای همه صفحات (در تاپ‌بار، و در صفحه ورود به‌صورت شناور)
- انتخاب کاربر در `localStorage` ذخیره می‌شود و **پس از بارگیری مجدد هم می‌ماند**
- اگر کاربر چیزی انتخاب نکرده باشد، همان تنظیم سیستم ملاک است
- تغییر رنگ‌ها با انتقال نرم (۰/۳ ثانیه) انجام می‌شود

رنگ‌های حالت شب همان پالتِ خود پروژه است (بدون رنگ‌سازی جدید):

| بخش | رنگ در حالت شب |
|---|---|
| پس‌زمینه اصلی | `#0a0f1a` |
| کارت‌ها و پنل‌ها | `#1e293b` |
| متن | `#f1f5f9` |
| سایدبار | گرادیان تیره |

## ۴. فایل‌های تغییرکرده

| فایل | تغییر |
|---|---|
| `admin/assets/style.css` | `@font-face` فونت‌ها، متغیرهای فونت، تبدیل بلوک تاریک به حالت قابل‌تغییر، استایل لوگو و کلید تم |
| `admin/assets/theme.js` | **جدید** — منطق کلید شب/روز و ذخیره انتخاب |
| `admin/assets/img/` | **جدید** — دو نسخه لوگو |
| `admin/assets/fonts/` | **جدید** — ۷ فایل فونت وب |
| ۲۰ فایل `admin/*.php` | افزودن اسکریپت تم |
| ۱۸ فایل `admin/*.php` | جایگزینی عنوان سایدبار با لوگو |
| `admin/login.php` | لوگو + تبدیل بلوک تاریک اختصاصی به حالت قابل‌تغییر |

## ۵. نتایج آزمون‌ها

| مورد | نتیجه |
|---|---|
| فونت متن (ایران‌سنس) | ✅ بارگذاری و اعمال شد |
| فونت عنوان (یکان) | ✅ بارگذاری و اعمال شد |
| لوگوی سایدبار | ✅ ۱۶۸×۷۷ پیکسل، فقط یک نسخه در هر تم |
| لوگوی صفحه ورود | ✅ ۲۴۰×۱۰۹ پیکسل |
| کلید تم در همه صفحات | ✅ (شامل ورود، گزارش‌ها، کاربران، واحدها، تنظیمات) |
| تغییر به حالت شب | ✅ پس‌زمینه `#0a0f1a`، کارت `#1e293b`، متن روشن |
| ماندگاری پس از بارگیری مجدد | ✅ انتخاب در localStorage ذخیره می‌شود |
| تعویض خودکار لوگو با تم | ✅ |
| خطای PHP / JS | ✅ صفر |
| داده‌ها | ✅ ۸ گزارش، ۵ کاربر، ۵۶ واحد اداری |
| بارگیری منابع جدید | ✅ همه ۲۰۰ |

## ۶. اگر خواستید شخصی‌سازی کنید

- **تعویض لوگو:** فایل جدید را با همین نام در `admin/assets/img/` بگذارید (`logo.png` و `logo-light.png`).
- **فعال کردن فونت کلمه:** فایل‌های woff2 را در `admin/assets/fonts/` قرار دهید و بگویید تا `@font-face` آن را اضافه کنم (یا خودتان مطابق الگوی موجود اضافه کنید).
- **تغییر رنگ حالت شب:** متغیرهای `--dark-*` در ابتدای `admin/assets/style.css`.

---

## ۷. رفع اشکال‌های گزارش‌شده در صفحه ورود

### الف) آیکون‌های کلید و کاربر روی متن فیلدها می‌افتادند

**علت:** در صفحات راست‌به‌چپ، متنِ فیلد از **سمت راست** شروع می‌شود. اما در استایل فیلدها فقط فضای سمت چپ رزرو شده بود (`padding-left: 48px` برای دکمه چشم) و آیکونِ کاربر/کلید هم در `right: 14px` قرار داشت — یعنی دقیقاً روی اولین حروف متن.

**اندازه‌گیری پیش از رفع:**

| مورد | مقدار |
|---|---|
| جایگاه آیکون | x = ۸۷۶ تا ۸۹۰ |
| شروع متن | x = ۸۸۸ |
| نتیجه | ❌ هم‌پوشانی |

**رفع:** افزودن `padding-right: 48px` به استایل فیلدها.

**پس از رفع:** شروع متن x = ۸۵۶، آیکون x = ۸۷۶ → **۲۰ پیکسل فاصله** ✅

### ب) کلید شب/روز روی موبایل روی جعبه ورود می‌افتاد

کلید به‌صورت شناور در گوشه صفحه بود و در صفحه‌نمای کوچک با لبه بالایی جعبه ورود تداخل داشت. کلید اکنون **درون کارت ورود** و در گوشه آن قرار می‌گیرد (در دسکتاپ، موبایل، روز و شب بدون تداخل).

### ج) کش مرورگر

برای اطمینان از بارگیری نسخه جدید استایل‌ها، نسخه منابع ارتقا یافت:

```
assets/style.css?v=3
assets/theme.js?v=3
```

اگر هنوز ظاهر قدیمی را می‌بینید، یک بار **بارگیری مجدد سخت** انجام دهید:
`Ctrl + Shift + R` (ویندوز/لینوکس) یا `Cmd + Shift + R` (مک).

### نتایج آزمون نهایی

| مورد | نتیجه |
|---|---|
| فاصله متن و آیکون (نام کاربری و رمز) | ✅ ۲۰ پیکسل، بدون هم‌پوشانی |
| کلید تم درون کارت، بدون تداخل | ✅ دسکتاپ و موبایل، روز و شب |
| ورود موفق | ✅ هر چهار حالت |
| لوگو | ✅ ۲۴۰×۱۰۹ پیکسل |
| فونت‌ها | ✅ ایران‌سنس و یکان |
| FontAwesome (آیکون‌ها) | ✅ بارگذاری شد |
| خطای PHP / JS | ✅ صفر |
